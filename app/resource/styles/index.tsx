import {ViewStyle} from 'react-native';
import {screenContainerBackgroundColor} from '@resource/colors';

export const screenContainerStyle: ViewStyle = {
  flex: 1,
  backgroundColor: screenContainerBackgroundColor,
  flexDirection: 'column',
};
