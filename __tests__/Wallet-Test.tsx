/**
 * @format
 */

import 'react-native';
import React from 'react';
import {generateWallet} from '../app/ether/wallet';

it('Ether钱包创建失败', async () => {
  // try {
  //   const result = await generateWallet();
  //   const {mnemonic, privateKey, publicKey} = result;
  //   console.log('获取到的钱包信息：', mnemonic, privateKey, publicKey);
  //   console.log();
  // } catch (error) {
  //   expect(error).toEqual({error: 'Ether钱包创建失败'});
  // }
});
